# 已知缺口（known gaps）

> 阶段 B 汇总。下列项**不算验收失败**：PRD 已标 `[待补充]` / `[待运营提供]`、设计 Out of scope，或实测中明确记为 GAP。

| ID | 来源 | 简述 | 为何不算失败 | 后续建议 |
| --- | --- | --- | --- | --- |
| GAP-T2-01 | F05/F06 | 课程与活动无库表关联（无 `courses.activity_id`） | PRD 关联能力未落到 V1 schema | 若产品需要回流活动，再加外键与详情入口 |
| GAP-T3-01 | F10 | 线下「结束后 24h 申请窗」在默认 seed 下不易当场观察 | seed 线下活动 `end_at` 多为未来时间 | 增加已结束线下活动 seed，或后台可改时间便于验收 |
| GAP-T4-01 | A04 / AC-A04-03 | 加入申请「通过」后**不会自动开户** | PRD 标明开户方式 `[待补充]` | 确认开户规则后实现创建/激活雏英账号 |
| ND-T03-01 | F09 | 错误重试 / 故障注入 UI 未做专项压测 | 可选能力，非主路径阻塞 | 需要时再做节流与错误态抽检 |
| OOS-MOBILE | 设计 Out of scope | 移动端适配 | V1 仅 PC Web | 另开迭代 |
| OOS-SSO | 设计 Out of scope | 企业 SSO / 真实身份源 | V1 用演示登录 + 账号密码 | 对接 IdP 时替换 auth |
| OOS-SHOP | 设计 Out of scope | 商城 | 未纳入 V1 | 产品单独立项 |
| CMS-OPS | 多页 `[待运营提供]` | 首页/计划介绍终稿文案与主视觉 | 结构位已就绪，允许占位 | 运营提供素材后在 CMS 发布 |
| QA-L1 | 验收测试 | Seed 仅创建用户/模板/内容，不含演示用的申请/报名数据 | 演示数据由手工或测试脚本生成 | 可增加 demo-data seed 脚本 |
| QA-L2 | 验收测试 | `/admin/point-apps` 列表显示原始用户 ID 而非显示名 | 不影响功能，仅可读性 | 后续加 JOIN 查询或前端映射 |
| QA-L3 | 验收测试 | `migrate.ts` 每次启动运行数据修复 UPDATE | 不影响已有数据，但可能覆盖管理员手动清除的 deadline | 改为版本化一次性迁移 |
| QA-L4 | 验收测试 | 密码使用 SHA-256 单次哈希（无盐、无迭代） | 演示环境可接受 | 生产部署前升级为 argon2id/bcrypt |
| QA-L5 | 验收测试 | 非生产环境 JWT 密钥回退为硬编码 `"dev-jwt-secret"` | 仅本地开发影响 | 生产环境通过 `JWT_SECRET` 环境变量注入 |
| QA-L6 | 验收测试 | 无全局 JSON 错误中间件，未处理异常泄漏 HTML/堆栈 | 当前所有路由均有 try/catch | 添加 Express 错误处理中间件 |
| QA-L7 | 验收测试 | 缺少 helmet 安全头（CSP, HSTS, X-Frame-Options 等） | 演示应用非公网暴露 | 生产部署前添加 helmet 中间件 |
| QA-L8 | 验收测试 | 登录端点无速率限制 | 演示环境单用户无影响 | 生产部署前添加 express-rate-limit |

## 已关闭的产品决策（阶段 B）

| 主题 | 结论 |
| --- | --- |
| 登录字段 | 仅账号 + 密码；加入申请联系方式保持单字段 |
| 报名截止 | 等于活动开始时间；前台/后台不单独展示「报名截止」列 |
| 积分申请通道截止 | 独立字段 `point_apply_deadline`，后台可配、前台展示并校验 |
| 用户停用 | 停用/启用前二次确认 |
| 产品正名 | 用户可见「雏鹰」→「雏英」；npm scope `@chuying/*` 保留 |
