# 雏英计划宣传官网 — 前端页面总览（globaluser）

> 版本：V1.6 | 日期：2026-08-05 | 终端：PC Web（不做移动端适配）  
> 设计依据：`docs/superpowers/specs/2026-08-02-chuying-portal-design.md`  
> 视觉真源：`docs/prd/globaluser-uiux-设计系统-方案A.md`（方案 A：Soft UI Evolution × 教育青绿）  
> 规范依据：`rules/prd.md`；页面文档体例对齐广推 learn-demo `globaluser-*.md`  
> **实现对照：** 本文 V1.6 起与仓库当前代码对齐；缺口见 `known-gaps.md`

---

## 1. 系统概述

雏英计划宣传官网是面向 SoftTong「雏英计划」的 **PC Web 全栈系统**：访客在前台首页同时看到**雏英计划宣传**、**软通智慧公司宣传**以及活动/课程精选入口；**雏英**完成报名与积分申请；**管理员 / 超级管理员**在独立后台完成内容（含宣传素材槽位）、审核、活动、积分与权限运营。宣传终稿素材由运营后续提供，文档中标 `[待运营提供]`。

**产品形态：** 同一站点双端入口——前台 `/`、后台 `/admin`  
**登录：** **账号（邮箱）+ 密码**为主路径；另提供一键演示三角色（雏英 / 管理员 / 超级管理员）  
**演示账号：** `eagle@demo` / `admin@demo` / `super@demo`，密码统一 `Demo1234!`  
**首期语言：** 简体中文  

**终端规格：** PC Web；Chrome 90+ / Edge 90+；分辨率 **1366×768** 及以上  

---

## 2. 用户角色体系

| 角色 | 使用端 | 权限范围 |
| --- | --- | --- |
| 访客 | 前台 | 只读浏览；可提交加入申请 |
| 雏英 | 前台 | 报名活动、提交积分申请、查看个人中心 |
| 管理员 | 后台 | 按已授予的权限包操作 |
| 超级管理员 | 后台 | 全部权限包 + 权限管理 |

### 2.1 后台权限包（V1 全部上线）

| 权限包 code | 名称 | 说明 |
| --- | --- | --- |
| `content` | 内容运营 | CMS 编辑与发布 |
| `join_review` | 加入审核 | 加入申请通过/驳回 |
| `activity` | 活动管理 | 活动 CRUD、报名名单、线上进度 |
| `point_type` | 积分类型 | 模板启用/停用与分值配置 |
| `point_review` | 积分审批 | 申请队列审批 |
| `user` | 用户管理 | 雏英与管理员账号状态 |
| `dashboard` | 数据看板 | 关键指标总览 |
| `permission` | 权限管理 | 仅超管：权限包与管理员授权 |

无权限包：侧栏不展示对应菜单；直链访问展示「无权限」页。

---

## 3. 页面架构总览

```
雏英计划宣传官网（PC Web）
│
├── 前台（公开壳：顶栏 + 页脚）
│   ├── 未登录 / 已登录共用浏览
│   │   ├── F01 首页（计划宣传 + 公司宣传 + 活动/课程精选）
│   │   ├── F02 计划介绍页
│   │   ├── F03 活动列表页
│   │   ├── F04 活动详情页
│   │   ├── F05 课程列表页
│   │   ├── F06 课程详情页
│   │   └── F07 加入我们页
│   ├── F08 登录页（演示一键三角色）
│   └── 已登录·雏英
│       ├── F09 个人中心概览
│       ├── F10 我的报名
│       ├── F11 我的申请
│       ├── F12 积分明细
│       ├── F13 发起积分申请
│       └── F14 申请详情
│
└── 管理后台（侧栏 + 顶栏；需管理员/超管）
    ├── A01 后台控制台
    ├── A02 内容运营
    ├── A03 加入审核列表
    ├── A04 加入审核详情
    ├── A05 活动管理列表
    ├── A06 活动编辑
    ├── A07 活动报名名单
    ├── A08 积分类型管理
    ├── A09 积分审批列表
    ├── A10 积分审批详情
    ├── A11 用户管理
    ├── A12 数据看板
    └── A13 权限管理（仅超管）
```

---

## 4. 页面路由与文档索引

| 编号 | 模块 | 页面 | 路由 | 文档 |
| --- | --- | --- | --- | --- |
| F01 | 首页 | 首页 | `/` | `globaluser-首页-首页.md`（含软通智慧宣传区） |
| F02 | 计划介绍 | 计划介绍页 | `/about` | `globaluser-计划介绍-计划介绍页.md` |
| F03 | 活动 | 活动列表页 | `/activities` | `globaluser-活动-活动列表页.md` |
| F04 | 活动 | 活动详情页 | `/activities/:id` | `globaluser-活动-活动详情页.md` |
| F05 | 课程 | 课程列表页 | `/courses` | `globaluser-课程-课程列表页.md` |
| F06 | 课程 | 课程详情页 | `/courses/:id` | `globaluser-课程-课程详情页.md` |
| F07 | 加入我们 | 申请加入页 | `/join` | `globaluser-加入我们-申请加入页.md` |
| F08 | 账号 | 登录页 | `/login` | `globaluser-账号-登录页.md` |
| F09 | 个人中心 | 概览 | `/me` | `globaluser-个人中心-概览.md` |
| F10 | 个人中心 | 我的报名 | `/me/enrollments` | `globaluser-个人中心-我的报名.md` |
| F11 | 个人中心 | 我的申请 | `/me/applications` | `globaluser-个人中心-我的申请.md` |
| F12 | 个人中心 | 积分明细 | `/me/points` | `globaluser-个人中心-积分明细.md` |
| F13 | 积分申请 | 发起申请 | `/me/applications/new` | `globaluser-积分申请-发起申请.md` |
| F14 | 积分申请 | 申请详情 | `/me/applications/:id` | `globaluser-积分申请-申请详情.md` |
| A01 | 后台 | 控制台 | `/admin` | `globaluser-后台-控制台.md` |
| A02 | 后台 | 内容运营 | `/admin/content` | `globaluser-后台-内容运营.md` |
| A03 | 后台 | 加入审核列表 | `/admin/join` | `globaluser-后台-加入审核列表.md` |
| A04 | 后台 | 加入审核详情 | `/admin/join/:id` | `globaluser-后台-加入审核详情.md` |
| A05 | 后台 | 活动管理列表 | `/admin/activities` | `globaluser-后台-活动管理列表.md` |
| A06 | 后台 | 活动编辑 | `/admin/activities/new`、`/admin/activities/:id/edit` | `globaluser-后台-活动编辑.md` |
| A07 | 后台 | 活动报名名单 | `/admin/activities/:id/enrollments` | `globaluser-后台-活动报名名单.md` |
| A08 | 后台 | 积分类型管理 | `/admin/point-types` | `globaluser-后台-积分类型管理.md` |
| A09 | 后台 | 积分审批列表 | `/admin/point-apps` | `globaluser-后台-积分审批列表.md` |
| A10 | 后台 | 积分审批详情 | `/admin/point-apps/:id` | `globaluser-后台-积分审批详情.md` |
| A11 | 后台 | 用户管理 | `/admin/users` | `globaluser-后台-用户管理.md` |
| A12 | 后台 | 数据看板 | `/admin/dashboard` | `globaluser-后台-数据看板.md` |
| A13 | 后台 | 权限管理 | `/admin/permissions` | `globaluser-后台-权限管理.md` |
| F15 | 前台 | 内容详情页 | `/content/:key` | `globaluser-首页-首页.md`（V1.4新增） |
| A14 | 后台 | 课程管理列表 | `/admin/courses` | `globaluser-后台-活动管理列表.md`（V1.4新增） |
| A15 | 后台 | 课程编辑 | `/admin/courses/new`、`/admin/courses/:id/edit` | `globaluser-后台-活动编辑.md`（V1.4新增） |
| A16 | 后台 | 课程报名名单 | `/admin/courses/:id/enrollments` | `globaluser-后台-活动报名名单.md`（V1.4新增） |
| A17 | 后台 | 知识库 | `/admin/kb` | 见 §5.9（V1.5 登记；独立 PRD 可后续补） |
| F16 | 前台 | 公司介绍 | `/about-company` | 见 §5.9（V1.5 登记） |

---

## 5. 全局交互规范

### 5.1 前台布局框架

```
┌──────────────────────────────────────────────────────────┐
│ 顶栏 (64px)：Logo「雏英计划」 | 导航 | [登录/头像▾]        │
├──────────────────────────────────────────────────────────┤
│                                                          │
│                  页面主内容（全宽或 max 1200px）            │
│                                                          │
├──────────────────────────────────────────────────────────┤
│ 页脚：计划简介摘要 | 链接 | © SoftTong                    │
└──────────────────────────────────────────────────────────┘
```

前台主导航：首页｜计划介绍｜活动｜课程｜加入我们；登录后增加「个人中心」。  
首页信息优先级：**计划宣传 → 公司宣传 → 精选活动/课程**（完整列表仍在活动/课程频道）。

### 5.2 后台布局框架

```
┌──────────────────────────────────────────────────────────┐
│ 顶栏：后台标题 | 当前账号 | 退出 / 回前台                  │
├────────────┬─────────────────────────────────────────────┤
│ 侧栏 220px │ 面包屑 + 标题 + 主操作                       │
│ 按权限包   │ 内容区（表格/表单，max 1280px）               │
│ 显隐菜单   │                                             │
└────────────┴─────────────────────────────────────────────┘
```

### 5.3 视觉与品牌（方案 A 摘要）

| 项 | 值 |
| --- | --- |
| 风格 | Soft UI Evolution |
| Primary | `#0D9488` |
| Accent（主转化 CTA） | `#D97706` |
| Background | `#F0FDFA` |
| 中文字体 | Noto Sans SC |
| 详细规范 | 见 `globaluser-uiux-设计系统-方案A.md`（Token、组件、壳、AC） |
| 比选记录 | `globaluser-uiux-风格推荐.md` |

各页面 PRD 的布局线框继续有效；**颜色/字体/组件态以设计系统 PRD 为准**。

### 5.4 通用交互规则

| 规则 | 说明 |
| --- | --- |
| 加载态 | 列表/详情异步加载用骨架屏 |
| 空状态 | 插画或简洁文案 + 引导 CTA |
| 错误态 | Toast + 可重试 |
| 表单 | 失焦校验 + 提交全量校验；错误贴字段下方 |
| 二次确认 | 驳回、下架、删除、停用等 |
| Toast | 成功/失败右上角，约 3 秒 |
| 权限 | 后台无权限菜单隐藏；直链无权限页 |

### 5.5 核心业务常量（全站一致 · 与代码 `@chuying/shared` 对齐）

| 常量 | 值 | 说明 |
| --- | --- | --- |
| 课程观看进度阈值 | **99%**（`WATCH_PROGRESS_THRESHOLD`） | **仅课程**类型一：进度 ≥99% 可申请心得 |
| 活动申请窗口 | **结束时间 + 24 小时**（`OFFLINE_APPLY_WINDOW_HOURS`） | **活动**（online/offline 同规则）：结束后 24h 内可申请；**不再**用进度卡活动心得 |
| 积分申请通道截止 | `point_apply_deadline` | 活动发布必填；与 24h 窗口取更早者；前台详情展示 |
| 报名截止 | **等于活动开始时间 `start_at`** | 开始后不可报名；后台/前台**不单独配置「报名截止」字段** |
| 心得字数 | **300–1000 字** | 类型一必填；按字符长度计 |
| 类型二模板 | contest_award / speech / project_contrib / honor / other_special | 预置，可启用停用 |

**活动 vs 课程（实现口径）：**

| 对象 | 报名 | 类型一门槛 |
| --- | --- | --- |
| 活动（线上/线下） | `now < start_at` | 已报名 + 活动已结束 + 处于 end+24h 且未超过 `point_apply_deadline` |
| 课程 | 已发布即可随时报名 | 已报名 + 观看进度 ≥99%（无结束时间窗） |

活动详情仍可展示/更新线上活动观看进度（演示用），但**该进度不作为活动心得申请门槛**。

### 5.6 积分申请类型

| 类型 | 名称 | 表单要点 |
| --- | --- | --- |
| 类型一 | 活动/课程完成心得 | 绑定已报名活动或课程；心得 300–1000 字；门槛见 §5.5 |
| 类型二 | 专项/比赛等 | 独立入口（仅雏英个人中心），先选模板再填事项+理由；默认分值来自模板；不绑定活动/课程 |

从活动/课程详情页直接申请时类型锁定为类型一，不可切换。审批：通过写流水；驳回必填原因。积分审批详情可调用 **AI 预审**（可选，DeepSeek）辅助审核类型一心得。

### 5.7 图片上传系统（V1.4）

| 项 | 值 |
| --- | --- |
| 上传接口 | `POST /api/admin/upload`（multipart/form-data，需管理员登录） |
| 存储位置 | `server/public/uploads/`（Git 跟踪，随仓库分发） |
| 支持格式 | PNG / JPEG / GIF / SVG / WebP |
| 文件大小 | ≤ 10MB |
| 访问路径 | `/uploads/<filename>`（Express 静态文件服务） |
| 前端组件 | `ImageUpload`：文件选择 + 自动上传 + URL 回显 |

内容块封面、活动图片、课程封面均使用此上传系统。

### 5.8 内容运营 CMS（V1.4）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| 标题 | 文本 | 内容块标题 |
| 简介 | 文本（≤500字） | 首页卡片摘要 |
| 封面图 | 上传图片 | 首页卡片展示 + 详情页头图 |
| 正文 | 富文本 HTML | 详情页完整内容（支持图文排版） |
| 跳转链接 | URL + 按钮文案 | 可选的了解更多按钮 |

Seed 已含 `home_hero` / `home_plan_promo` / `home_company_promo`。计划介绍页（`/about`）读取已发布的 `home_plan_promo`；`/content/:key` 展示单块详情。

**实现注意（V1.5）：** 当前首页 `/` 主视觉与宣传区仍以**前端占位/本地默认文案**为主，未完整消费 CMS 列表接口；公司介绍 `/about-company` 为静态页。后台 CMS 已可编辑发布，前台首页与 CMS 的完全打通记入 `known-gaps.md`（CMS-HOME）。

### 5.9 扩展能力（已实现 · 原 V1 索引外）

| 能力 | 路由 / 入口 | 说明 |
| --- | --- | --- |
| 公司介绍页 | `/about-company` | 静态公司宣传；首页有入口，顶栏未单列 |
| 知识库管理 | `/admin/kb`（需 `content`） | 后台维护问答/文档，供前台助手检索 |
| 前台智能助手 | 门户浮层 `ChatWidget` | 登录后可聊；依赖知识库与模型配置 |
| 加入通过开户 | A04 通过操作 | **contact 须为邮箱**；通过时若不存在对应用户则创建雏英账号并回显一次性密码 |
| 用户停用 | A11 | 停用/启用前二次确认；停用后 JWT 受保护接口 401 |

---

## 6. 导航与鉴权规则

| 场景 | 行为 |
| --- | --- |
| 访客访问需登录页 | 跳转 `/login?redirect=` |
| 雏英访问 `/admin/*` | 拒绝，提示无权限 |
| 管理员访问前台个人能力 | 可浏览公开页；积分/报名能力以角色为准（演示账号分流） |
| 登录成功 | 雏英 → `/me` 或 redirect；管理端角色 → `/admin` |

---

## 7. 分页面文档撰写约定

每份 `globaluser-*.md` 至少包含：

1. 页面概述（路由、角色、目的、类型）  
2. ASCII 线框布局（PC）  
3. 区域说明与交互（含空/错/加载）  
4. 关键字段与校验  
5. 主流程 / 分支 / 异常  
6. 接口依赖摘要（方法 + 路径意图即可）  
7. 验收要点（可测 AC）  

禁止与本总览常量冲突；缺业务数据标 `[待补充]`，禁止编造对外指标。

---

## 8. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-02 | V1.0：按已批准设计规格建立页面总览与文档索引 |
| 2026-08-02 | V1.1：明确首页职责含雏英计划宣传 + 软通智慧公司宣传；活动/课程为精选入口 |
| 2026-08-02 | V1.2：视觉定案方案 A；索引设计系统 PRD |
| 2026-08-03 | V1.3：明确线上→课程（视频≥99%即申请）、线下→活动（结束+24h）的定位区分；更新 §5.5–5.6 |
| 2026-08-05 | V1.4：积分类型增删管理；活动/课程分离（线上video/线下image/课程视频进度）；积分类型锁定；心得300–1000字；内容运营富文本+图片上传+详情页；超管增删管理员；雏英开户自动生成账号；数据看板可点击卡片+趋势图；全局UI修复（进度输入/弹窗滚动/下拉框/回前台删除）；管理员积分详情补全 |
| 2026-08-05 | V1.5：**对照当前代码校正**——活动报名=开始前；活动心得=结束+24h∩通道截止（不要求进度99%）；课程心得=进度≥99%；登录账号密码为主+演示一键；加入 contact=邮箱且通过可自动开户；登记知识库/聊天助手/公司介绍；首页 CMS 消费缺口写入 known-gaps |
| 2026-08-05 | V1.6：数据看板全面升级(AI运营洞察+待办队列+Sparkline+双图+Hover+明细抽屉)；设计token体系(蓝版)；性能优化(compression+cache+vendor分包)；首页升级(Hero轮播+纵向时间线+业务卡片+4人风采+2026轮播+公司介绍页)；AI审核辅助(DeepSeek预审)；AI问答助手+RAG知识库；设计系统(global.css+tokens.css) |
